//
// Created by hp.he on 2019/3/20.
//

#include "PointNodesComparison.h"

float PointNodesComparison::simpleCalculateAccuracy(vector<PointNodeStructure> groundTruth,
                                                    vector<PointNodeStructure> others) {

    // simple for
    for (int i = 0; i < 0; ++i) {
        for (int j = 0; j < 0; ++j) {
            for (int k = 0; k < 0; ++k) {

            }
        }
    }
}


/**
 * 八叉树
 * */
float PointNodesComparison::ocalTreeCalculateAccuracy(vector<PointNodeStructure> groundTruth,
                                                      vector<PointNodeStructure> others) {}
